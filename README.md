I just finished a project that connects computer architecture theory with data science.I wrote C programs to measure how CPU cache affects data processing speed — and the result was a 7x speedup just by changing the order of memory access.Using what I learned in my Computer Architecture module at UOK, I ran 4 experiments, collected timing data, and analyzed it in Python with pandas and matplotlib.Key finding: cache-friendly code ran 7x faster than cache-unfriendly code — same task, same computer, just different memory access patterns.This is why tools like NumPy are so fast. They are written to be cache-friendly inside
